# Import the concrete adapter you need directly, e.g.:
#   from adapters.elf_adapter import ELFAdapter
#   from adapters.langflow_adapter import LangFlowAdapter
# Not re-exported here so that using one backend never requires the other's
# dependencies (LangFlow package / ELF-torch source tree) to be importable.
