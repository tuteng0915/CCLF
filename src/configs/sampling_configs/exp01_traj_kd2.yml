# EXP-01 Protocol B for kd2 checkpoint — save per-step trajectory states.
- sampling_method: ode
  num_sampling_steps: [32]
  cfgs: [1]
  self_cond_cfg_scales: [1]
  time_schedule: uniform
  dec_sc_mode: "none"
  save_trajectory: true
  trajectory_save_dir: "results/exp01_kd2/trajectories"
